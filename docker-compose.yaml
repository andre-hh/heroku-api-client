version: "3.4"

services:
    heroku-api-client-cli:
        build:
            context: .
            dockerfile: .docker/Dockerfile
        container_name: heroku-api-client-cli
        volumes:
            - .:/var/www/html:delegated
